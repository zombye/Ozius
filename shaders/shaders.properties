clouds = off

oldLighting = false

##--## Profiles

# Currently just to get the profile selector to show up until I actually create some profiles
profile.DEFAULT =

##--## Screens

screen = <profile> [MISC] [BLOOM] [SURFACE] [SHADOWS] [GI] [VL]
screen.MISC = *

screen.BLOOM = BLOOM BLOOM_AMOUNT
screen.SURFACE = TEXTURE_RESOLUTION [PARALLAX]
screen.PARALLAX = PM PM_DEPTH PM_STEPS PM_DIST PSS PSS_STEPS
screen.SHADOWS = SHADOW_SAMPLING_TYPE shadowMapResolution shadowDistance [HSSRS]
screen.HSSRS = HSSRS HSSRS_RAY_STEPS HSSRS_RAY_LENGTH
screen.GI = GI GI_SAMPLES GI_RESOLUTION GI_RADIUS
screen.VL = VL VL_STEPS VL_MULT
