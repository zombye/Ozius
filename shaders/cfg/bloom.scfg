#define BLOOM
#define BLOOM_AMOUNT 0.25 // Adjusts the blend between bloom and no bloom. [0.15 0.20 0.25 0.30 0.35]

#define BLOOM_RADIUS 3   // Adjusts the radius used during tile calculation. [3 5 7]
#define BLOOM_CURVE  2.0 // Power curve for the weight of each tile. pow(<tile>, -BLOOM_CURVE). Larger values give a more focused result. [0.5 1.0 1.5 2.0 2.5 3.0 3.5]
