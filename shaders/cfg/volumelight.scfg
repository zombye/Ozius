#define VL

#define VL_STEPS        8     // Steps to take. [4 6 8 12 16]
#define VL_MAX_DISTANCE 128.0 // Render distance.
#define VL_MULT         1e3   // Only realistic if set to 1.0, but it looks better if it's set to a (much) higher value.
